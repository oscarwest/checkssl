{
  "name": "checkssl",
  "version": "1.0.0",
  "description": "With the good work by \"Let’s Encrypt\" in providing free SSL certs for users, I wanted a quick way to check all the domains I look after to determine which ones have correct SSL certs, and which ones are in need of updating etc.",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "debug": "node --trace-warnings index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/oscarwest/checkssl.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/oscarwest/checkssl/issues"
  },
  "homepage": "https://github.com/oscarwest/checkssl#readme",
  "dependencies": {
    "express": "^4.16.4",
    "shelljs": "^0.8.2",
    "valid-url": "^1.0.9"
  }
}
